# ==============================================================================
# CampusMarket Frontend — Environment Variables Template
# ==============================================================================
# Optional: In development, Vite automatically proxies /api to http://localhost:5000.
# Only specify VITE_API_URL if connecting to a custom backend host or production URL.
# ==============================================================================

VITE_API_URL=/api/v1
